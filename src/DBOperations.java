public class DBOperations {
    //Som jeg forstår så skal der være en seperat klasse der står for DB... I ændre bare hvis ikke

    /*Public boolean rentCar(){
        Car car = getCar(int x);
        Renter renter = getRenter(int x);
        INSERT INTO contract(y,y,y,y,y,y)
        VALUES (car.getPlate, car,getWhatevs, renter.getName, renter.getDriversLicense etc.)
        send query;

        //Returns true if successful
    }*/

    /*Public Car getCar(int x){
        SELECT * FROM car WHERE plate = ?;
        noget med query statement (1,x) ;
        Send query,  hent result, gem results i plate = result.1 or something like that.
        Car car = new Car(1,x,x,x,x,x):
        return car
        }
     */

    /*public Renter getRenter(int x){
        SELECT * FROM renter WHERE renter_id = ?;
        noget med query statement (1,x) ;
        Send query,  hent result, gem results i plate = result.1 or something like that.
        Renter renter = new Renter(1,x,x,x,x);
        return renter;
    }*/



}
